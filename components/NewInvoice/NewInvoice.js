const NewInvoice = (props) => {
  return (
    // find maybe something to install that makes customizable fors
    // mainly because of the date
    // styling would also be a pain
    // maybe try it by hand idk YOLO

    <div>
      <form action="">
        <label htmlFor=""></label>
        <input type="text" />
      </form>
    </div>
  );
};

export default NewInvoice;
